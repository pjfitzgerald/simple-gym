services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - app_data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_PATH=/app/data/simple-gym.db
    restart: unless-stopped

volumes:
  app_data:
